/* ============================================================
 * 30_enhance_armament_base.dtsi — 〈Enhance Armament〉基礎術式
 *
 * 武装完全支配術 ─ 基礎behavior定義（神器の挙動を司る根源層）
 * behaviors { } の内側でインクルードされる。
 * hold-tap / sensor-rotate / sticky-key / tri-state 系。
 * ============================================================ */

gesture_mo_kp: gesture_mo_kp {
    compatible = "zmk,behavior-hold-tap";
    label = "GESTURE_MO_KP";
    bindings = <&mo>, <&kp>;

    #binding-cells = <2>;
    flavor = "tap-preferred";
    tapping-term-ms = <210>;
    quick-tap-ms = <150>;
    require-prior-idle-ms = <450>;
};

rotate: rotate {
    compatible = "zmk,behavior-sensor-rotate";
    label = "ROTATE";
    #sensor-binding-cells = <0>;
    bindings = <&msc MOVE_UP>, <&msc MOVE_DOWN>;

    tap-ms = <20>;
};

lt_mkp: lt_mkp {
    compatible = "zmk,behavior-hold-tap";
    label = "LT_MKP";
    bindings = <&mo>, <&mkp>;

    #binding-cells = <2>;
    flavor = "balanced";
    tapping-term-ms = <200>;
    quick-tap-ms = <150>;
    require-prior-idle-ms = <150>;
};

mod_mkp: mod_mkp {
    compatible = "zmk,behavior-hold-tap";
    label = "MOD_MKP";
    bindings = <&kp>, <&mkp>;

    #binding-cells = <2>;
    flavor = "balanced";
    tapping-term-ms = <200>;
    quick-tap-ms = <150>;
    require-prior-idle-ms = <150>;
};

dragkey: dragkey {
    compatible = "zmk,behavior-hold-tap";
    label = "DRAGKEY";
    bindings = <&drag_on>, <&mkp>;

    #binding-cells = <2>;
    tapping-term-ms = <200>;
    require-prior-idle-ms = <150>;
    flavor = "tap-preferred";
    quick-tap-ms = <100>;
};

g_shft: g_shft {
    compatible = "zmk,behavior-sticky-key";
    #binding-cells = <1>;
    release-after-ms = <1000>;
    bindings = <&kp>;
};

swapper: swapper {
    compatible = "zmk,behavior-tri-state";
    label = "SWAPPER";
    #binding-cells = <0>;
    bindings = <&kt LGUI>, <&kp TAB>, <&kt LGUI>;

    ignored-key-positions = <10 15>;
};

swapper_rev: swapper_rev {
    compatible = "zmk,behavior-tri-state";
    label = "SWAPPER_REV";
    #binding-cells = <0>;
    bindings = <&kt LGUI>, <&kp LS(TAB)>, <&kt LGUI>;

    ignored-key-positions = <10 15>;
};
